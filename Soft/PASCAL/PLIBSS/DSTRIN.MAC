	.TITLE	DSTRIN
.INCLUDE	"P12MAC"
DSTRIN::
	MOV	R0,-(SP)
	CLR	R0
	MOV	6(SP),R0
	ASH	#4,R0
	ADD	8.(SP),R0
;	BISB	8.(SP),R0
	MOVB	R0,@4(SP)
	DSTRIN	4(SP)
	MOV	(SP)+,R0
	MOV	(SP)+,4(SP)
	CMP	(SP)+,(SP)+
	RETURN
	.END
